#!/usr/bin/env bash
#
# Vagrant Aliases

#
# DEFAULT
#

# # SSH into Vagrant box
# alias vm="vagrant ssh"
# alias vssh="vagrant ssh"

# # Vagrant-Up box
# alias vup="vagrant up"

# # Vagrant-Up and Provision box
# alias vupp="vagrant up --provision"

# # Vagrant-Provision box
# alias vprov="vagrant reload --provision"

# # Vagrant-Suspend box
# alias vdown="vagrant suspend"

# # Vagrant-Halt box
# alias vhalt="vagrant halt"

# # Vagrant-Destroy box
# alias vdone="vagrant destroy --force"

#
# USER
#

# # SSH into Vagrant box
# alias vm="(cd ~/Vagrant; vagrant ssh)"
# alias vssh="(cd ~/Vagrant; vagrant ssh)"

# # Vagrant-Up box
# alias vup="(cd ~/Vagrant; vagrant up)"

# # Vagrant-Up and Provision box
# alias vupp="(cd ~/Vagrant; vagrant up --provision)"

# # Vagrant-Provision box
# alias vprov="(cd ~/Vagrant; vagrant reload --provision)"

# # Vagrant-Suspend box
# alias vdown="(cd ~/Vagrant; vagrant suspend)"

# # Vagrant-Halt box
# alias vhalt="(cd ~/Vagrant; vagrant halt)"

# # Vagrant-Destroy box
# alias vdone="(cd ~/Vagrant; vagrant destroy --force)"

# # Edit Puphpet config file
# alias vconf="(cd ~/Vagrant/puphpet; ${EDITOR} config.yaml)"

# # Edit Vagrantfile
# alias vfile="(cd ~/Vagrant; ${EDITOR} Vagrantfile)"

#
# LARAVAL HOMESTEAD
#

# SSH into Vagrant Homestead box
#alias vm="(cd ~/Homestead; vagrant ssh)"
#alias vssh="(cd ~/Homestead; vagrant ssh)"

# Vagrant-Up Homestead box
#alias vup="(cd ~/Homestead; vagrant up)"

# Vagrant-Up and Provision Homestead box
#alias vupp="(cd ~/Homestead; vagrant up --provision)"

# Vagrant-Provision Homestead box
#alias vprov="(cd ~/Homestead; vagrant reload --provision)"

# Vagrant-Suspend Homestead box
#alias vdown="(cd ~/Homestead; vagrant suspend)"

# Vagrant-Halt Homestead box
#alias vhalt="(cd ~/Homestead; vagrant halt)"

# Vagrant-Destroy Homestead box
#alias vdone="(cd ~/Homestead; vagrant destroy --force)"

# Edit Homestead.yaml
#alias vhome="(cd ~/.homestead; ${EDITOR} Homestead.yaml)"

# Edit Homestead Vagrantfile
#alias vfile="(cd ~/Homestead; ${EDITOR} Vagrantfile.yaml)"
